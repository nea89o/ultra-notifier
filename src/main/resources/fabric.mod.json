{
  "schemaVersion": 1,
  "id": "ultranotifier",
  "name": "${modName}",
  "description": "${description}",
  "version": "${version}",
  "mixins": [
    "mixins.ultranotifier.json"
  ],
  "entrypoints": {
    "main": ["moe.nea.ultranotifier.UltraNotifierEntryPoint"]
  },
  "depends": {
    "minecraft": "~${mcVersion}",
    "java": ">=${java}"
  }
}